<%= form_tag request.path, :method => 'get' do %>
  <%= content_tag :label do %>
    Search user's name: <br>
    <%= text_field_tag :search, params[:search] %>
  <% end %>
  <%= submit_tag("Search") %>
<% end %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate @users %>